apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: service-desk-ingress
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: service-desk-service
                port:
                  number: 80
