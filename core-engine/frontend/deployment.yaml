apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: viniciusmoraodev/core-engine-frontend:v1.1.11
          imagePullPolicy: Always
          ports:
            - containerPort: 80
